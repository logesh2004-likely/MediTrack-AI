{
  "name": "healthcare-ai-adherence",
  "version": "1.0.0",
  "scripts": {
    "dev:all": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ai\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai": "cd ai_agent && .\\.venv\\Scripts\\python -m uvicorn main:app --reload --port 8000",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../ai_agent && python -m venv .venv && .\\.venv\\Scripts\\pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
